#compdef mkdir
# -----------
# Description
# -----------
#
# Completion script for mkdir
#
# ---------------------------------------------
# Author
# -------
#
#
#
# -------------------------------

_arguments -s \
	'(-m --mode)'{-m,--mode}'[Set permissions for new directory]'
	'(-p --parent)'{-p,--parent}'[Make parent directories if they don't exist]'
	'(-h --help)'{-h,--help}'[Show help message and quit]'
	'(-V --version)'{-V,--version}'[Show version number and quit]'