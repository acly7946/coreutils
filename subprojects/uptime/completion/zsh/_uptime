#compdef uptime
# -----------
# Description
# -----------
#
# Completion script for uptime
#
# ---------------------------------------------
# Author
# -------
#
#
#
# -------------------------------

_arguments -s \
	'(-p --pretty)'{-p,--pretty}'[Print in a human-readable format]'
	'(-s --since)'{-s,--since}'[Print time uptime is calculated from]'
	'(-h --help)'{-h,--help}'[Show help message and quit]'
	'(-V --version)'{-V,--version}'[Show version number and quit]'