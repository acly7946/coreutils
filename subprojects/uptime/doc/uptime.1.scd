uptime(1)

# NAME

uptime - output system uptime

# SYNOPSIS
*uptime* [OPTIONS]

# OPTIONS

*-p*, *--pretty*
	Print in a human-readable format

*-s*, *--since*
	Print time uptime is calculated from

*-h*, *--help*
	Show help message and quit

*-V*, *--version*
	Show version number and quit